package ua.shpp.eqbot.command;

import org.telegram.telegrambots.meta.api.objects.Update;

public class PressButtonTimeOrDateCommand implements ICommand {

    @Override
    public boolean execute(Update update) {
        return false;
    }
}
